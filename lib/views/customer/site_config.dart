import 'package:flutter/material.dart';

class SiteConfig extends StatelessWidget {
  const SiteConfig({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Site config screen'));
  }

}
