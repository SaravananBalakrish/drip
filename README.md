# oro_drip_irrigation

A new Flutter project.

# Build command
- Build debug
```bash
flutter build appbundle --flavor oroProduction -t lib/main_oroProduction.dart --build-name=1.0.0 --build-number=2
```
